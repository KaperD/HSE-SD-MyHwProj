<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>New homework</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/jquery-ui-dist@1.13.1/jquery-ui.min.css">
</head>
<body>
<nav>
    <div class="container pt-3">
        <nav class="navbar navbar-expand-md navbar-light">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler"
                        aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarToggler">
                    <ul class="navbar-nav align-content-center">
                        <li class="nav-item mt-3">
                            <div class="container-fluid">
                                <div class="row">
                                    <div class="col-auto ">
                                        <button class="btn rounded-pill bg-primary">
                                            <a class="nav-link active text-white" aria-current="page"
                                               href="/teacher/homeworks">Home</a>
                                        </button>
                                    </div>
                                    <div class="col-auto">
                                        <div class="nav-item mt-3">
                                            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32"
                                                 fill="currentColor" class="bi bi-arrow-right" viewBox="0 0 16 16">
                                                <path fill-rule="evenodd"
                                                      d="M1 8a.5.5 0 0 1 .5-.5h11.793l-3.147-3.146a.5.5 0 0 1 .708-.708l4 4a.5.5 0 0 1 0 .708l-4 4a.5.5 0 0 1-.708-.708L13.293 8.5H1.5A.5.5 0 0 1 1 8z"/>
                                            </svg>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </li>
                        <li class="nav-item mt-3 ms-3">
                            <button type="button" class="btn rounded-pill btn-outline-dark" disabled>
                                <a class="nav-link text-black" href="#">New homework</a>
                            </button>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </div>
</nav>
<main>
    <div class="container text-center pt-4 pb-0 ">
        <div class="row ">
            <div class="col-12 text-start">
                <h1 class="display-3">New homework</h1>
            </div>
        </div>
        <hr/>
    </div>
    <div id="formHolder" class="container pt-3 mb-5">
        <form>
            <div class="container-fluid border border-dark rounded-3 border-2 p-4">
                <div class="form-group py-2">
                    <label for="homeworkName" class="h4">Name:</label>
                    <input type="text" class="form-control rounded-3 border border-dark" id="homeworkName">
                </div>
                <div class="form-group py-2">
                    <label for="statement" class="h4">Statement:</label>
                    <textarea class="form-control rounded-3 border border-dark" id="statement" rows="3"></textarea>
                </div>
                <div class="form-group py-2">
                    <label for="check" class="h4">Check:</label>
                    <textarea class="form-control rounded-3 border border-dark" id="check" rows="3"></textarea>
                </div>
                <div class="form-group">
                    <div class="row">
                        <div class="col-sm-6 py-2">
                            <label for="publicationDatetime" class="h4">Publication date:</label>
                            <input type="datetime-local" class="form-control rounded-3 border border-dark"
                                   id="publicationDatetime">
                        </div>
                        <div class="col-sm-6 py-2">
                            <label for="deadline" class="h4">Deadline:</label>
                            <input type="datetime-local" class="form-control rounded-3 border border-dark"
                                   id="deadline">
                        </div>
                    </div>
                </div>
            </div>
            <div id="createHolder" class="d-flex pt-4 align-items-center">
                <div id="create" type="submit" class="btn btn-outline-dark display-6 rounded-pill"
                     aria-disabled="false">
                    <div class="display-6">Create</div>
                </div>
            </div>
        </form>
    </div>
</main>
<script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/jquery-ui-dist@1.13.1/jquery-ui.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
<script>
    function showLoading(createHolder, createButton) {
        createHolder.append(
            '<div class="spinner-border ms-3" role="status"></div>'
        );
        createButton.attr('aria-disabled', 'true');
        createButton.addClass("disabled");
    }

    function isLoading(createHolder, createButton) {
        return createButton.attr('aria-disabled') === 'true';
    }

    function removeLoading(createHolder, createButton) {
        createHolder.children().last().remove();
        createButton.attr('aria-disabled', 'false');
        createButton.removeClass("disabled");
    }

    function prepareDatetime(datetime) {
        if (datetime === "") {
            return "";
        }
        return (new Date(datetime)).toISOString();
    }

    function successCreation(data) {
        location.replace("/teacher/homeworks");
    }

    function failedCreation(data) {
        const alertCode = `
        <div id="alert" class="alert alert-danger d-flex align-items-center alert-dismissible fade show border border-danger border-2" role="alert">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-exclamation-triangle-fill red" viewBox="0 0 16 16">
                <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
            </svg>
            <div class="ms-3">
                The homework cannot be created with the provided data
            </div>
            <button id="alertClose" type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    `
        const alertBlock = $("#alert");
        if (alertBlock.length !== 0) {
            alertBlock.effect("shake");
        } else {
            $("form").before(alertCode);
        }
    }

    $(document).ready(function () {
        $("#create[aria-disabled='false']").click(function () {
            const createHolder = $("#createHolder");
            const createButton = $(this);

            if (isLoading(createHolder, createButton)) {
                return;
            }

            showLoading(createHolder, createButton);

            const publicationDatetime = prepareDatetime($("#publicationDatetime").val());
            const deadline = prepareDatetime($("#deadline").val());

            const newHomework = {
                name: $("#homeworkName").val(),
                publicationDatetime: publicationDatetime,
                check: $("#check").val(),
                task: $("#statement").val(),
                deadline: deadline
            };

            $.post({
                url: "/v1/teacher/homeworks",
                data: JSON.stringify(newHomework),
                contentType: "application/json",
                dataType: "json"
            }).done(successCreation).fail(function (data) {
                removeLoading(createHolder, createButton);
                failedCreation(data)
            });
        });
    });
</script>
</body>
</html>